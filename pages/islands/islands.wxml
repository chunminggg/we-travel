<view class="page">
    <view class='islands'>
    
    <image wx:for="{{itemArray}}" wx:key="*this" class="image-group" data-title="{{item.name}}" data-type="{{item.id}}" mode="aspectFill" src="{{item.lineImage}}" data-img="{{item.lineImage}}" bindtap="clickImageidx"></image>
    
    </view>
</view>